# 将棋 定石学習アプリ 設計書

> ステータス: **設計レビュー中(実装未着手)**
> 設計: Opus 4.8 / 実装予定: Sonnet 5
> 最終更新: 2026-07-26

---

## 0. この文書の位置づけ

Sonnet 5 が実装するための「唯一の正」となる設計書。ここに書かれたデータモデル・
アーキテクチャ・フェーズ順に沿って実装する。曖昧な箇所は実装前に必ず確認する。

---

## 1. 目的とスコープ

### 1.1 プロダクトの目的
特定戦法の定石を体系的に学べる将棋学習アプリ。ユーザーは、

1. アプリを起動する
2. 指したい戦法を選ぶ
3. その戦法で、対・各種戦法の **先手/後手 両方の手順** を学べる
4. **理想陣形までの組み方** と、**相手が定石から外れた場合の対処法** を学べる

### 1.2 決定済みの方針(2026-07-26 合意)
| 項目 | 決定 |
|---|---|
| プラットフォーム | **Web 専用**(React + Vite + TypeScript) |
| 逸れ手への対応 | **ハイブリッド**(主要定石=台本 / よくある逸れ=代表パターン台本 / それ以外=将棋エンジン) |
| v1 の最初の対抗形 | **居飛車 vs 四間飛車** |

### 1.3 v1 スコープ(MVP)
- 対抗形は **居飛車 vs 四間飛車 の 1 つ**に限定
- 自分が **居飛車側**の手順(先手/後手)を学べる
  - ※四間飛車側の手順は v2 以降(データ追加のみで対応できる設計にする)
- 学習モード(なぞり)+ 練習モード(実戦)
- エンジンによる逸れ手フォールバック
- 進捗の永続化は localStorage(アカウント/バックエンドなし)

### 1.4 v1 で「やらないこと」
- 複数対抗形の網羅(データ拡張で後追い)
- ログイン/クラウド同期(Firebase 等)
- モバイルネイティブ化(将来 Capacitor で検討)
- 対局 AI との自由対局(あくまで定石学習が主目的)

---

## 2. 技術スタック

| 領域 | 採用 | 理由 |
|---|---|---|
| 言語/ビルド | React + Vite + TypeScript | 軽量・高速・Web 完結 |
| 将棋ロジック | **tsshogi @2.3.4**(MIT) | SFEN/KIF/USI・棋譜ツリーを TS で扱える(ShogiHome 作者製) |
| 盤 UI | **自前 React コンポーネント** | ~~shogiground~~ は GPL-3.0 のため不採用(下記)。mockup 流用で自作 |
| 将棋エンジン | **YaneuraOu WASM**(`yaneuraou.wasm`) | ブラウザで USI エンジンを動かせる。逸れ手の最善応手・評価に使用(Phase 3) |
| 状態管理 | **Zustand @5**(MIT) | ユーザーが慣れている。軽量 |
| データ | 静的 JSON(バンドル) | v1 はバックエンド不要 |
| 駒フォント | **Yuji Syuku**(SIL OFL 1.1) | 楷書系・同梱/再配布可。`public/fonts/` に同梱 |
| スタイル | CSS Modules + `tokens.css` | mockup の CSS 変数を移植 |

> **盤 UI に shogiground を使わない理由(2026-07-26 決定)**:shogiground は
> **GPL-3.0-or-later**。Web 配布時にアプリ全体(定石データ・解説文含む)の
> ソース公開義務が生じる。当アプリの操作は「クリック選択→着手」「なぞり」中心で
> shogiground の旨み(ドラッグ&アニメ)が小さく、mockup 既存 CSS の流用で自作コストも
> 低いため、**自前 Board** を採用しライセンスを自由に保つ。
>
> **tsshogi の注意**:現局面の合法手を一括列挙する API は無い。自分の駒がある升 ×
> 全升 を `createMove` + `isValidMove` で総当たりして dests を作る自前ロジックが必要
> (9×9 なので性能問題なし)。打ち歩詰め等は `isPawnDropMate` 等で別途確認。

---

## 3. ドメインモデル(設計の中核)

### 3.1 局面・手の表現
- **局面 = SFEN 文字列**(tsshogi で復元/合法手生成)
- **手 = USI 形式**(例 `7g7f`, 打ちは `P*5f`)
- 表示用の日本語表記(`▲７六歩`)は tsshogi で USI から生成

### 3.2 定石データモデル

定石は「対抗形コース」単位で持つ。コースは **局面ノードの木(分岐付き)**。

```ts
// コース = 1 つの対抗形 × 自分の手番
interface JosekiCourse {
  id: string;                 // "ibisha-vs-shikenbisha--sente"
  title: string;              // "居飛車 vs 四間飛車(先手・急戦)"
  myStrategy: StrategyId;     // "ibisha"
  opponentStrategy: StrategyId; // "shikenbisha"
  mySide: "sente" | "gote";
  source?: string;            // 手順の出所(定跡書・サイト等の裏取り情報。DB自体は同梱しない)
  goalFormation: string;      // 理想陣形の説明(例: 舟囲い+棒銀の理想形)
  goalSfen?: string;          // 理想陣形に到達した局面(達成判定用)
  root: JosekiNode;
}

interface JosekiNode {
  id: string;
  sfen: string;               // このノードの局面
  comment?: string;           // この局面の教育的解説(自作)
  branches: JosekiMove[];     // ここから指せる手(定石/逸れ)
}

interface JosekiMove {
  usi: string;                // 指し手
  kind: "main" | "alt" | "deviation";
  //  main     = 定石本線
  //  alt      = 定石内の有力な別手順
  //  deviation= 相手のよくある逸れ手(咎め方を持つ)
  note?: string;              // この手の意味 / 逸れ手なら「なぜ悪いか」
  punishNote?: string;        // deviation のとき「どう咎めるか」の要点
  child: JosekiNode | null;   // null = 末端 or エンジンに委譲
}
```

### 3.3 3 層の応手システム(ハイブリッドの実体)

相手(または自分)の手に対する応答は次の優先順で解決する:

1. **本線・別手順(main/alt)** … 台本ノードをたどる
2. **代表的な逸れ手(deviation)** … 台本の「咎め方」ノードへ
3. **上記以外の未知の手** … `child == null` に落ちる →
   **将棋エンジンにフォールバック**し、
   - 相手の手の評価値
   - こちらの最善応手(USI)
   - テンプレ+評価に基づく解説(「この手は定石を外れています。ここは▲◯◯が最善で、狙いは〜」)
   を生成して提示する

> これにより「全逸れ手を手書きする」破綻を回避しつつ、
> **どんな手にも一応の答えを返せる**を両立する。

### 3.4 戦法マスタ
```ts
type StrategyId = "ibisha" | "shikenbisha" | /* v2+ */ "sangenbisha" | ...;
// ブラウズ用カテゴリ(§5.1 のフィルタと一致)
type Category = "ibisha" | "furibisha" | "nakabisha" | "kishu";
interface Strategy {
  id: StrategyId;
  name: string;         // "居飛車"
  kana: string;         // "いびしゃ"
  category: Category;   // 居飛車/振り飛車/中飛車/奇襲戦法
  popularity: number;   // 人気順ソート・★表示用
  level: string;        // "入門〜" 等(「初心者向け」フィルタ判定に使用)
  lineCount: number;    // 収録ライン数(カードに表示)
  ready: boolean;       // 定石データありか(false=準備中)
  description: string;
}
```

---

## 4. 学習/練習の 2 モード

### 4.1 学習モード(なぞり)
- アプリが本線を 1 手ずつ提示、ユーザーは「次へ」で進める
- 各局面の `comment` / 各手の `note` を解説パネルに表示
- 分岐点では別手順(alt)や相手の逸れ手(deviation)へ切り替え可能
- 目的:定石の流れと理想陣形を「見て理解する」

### 4.2 練習モード(実戦)
- ユーザーは自分の手番だけを実際に指す
- 相手(アプリ)は選択中コースのラインを指す
- ユーザーが **定石と違う手**を指したら:本来の定石手と理由を提示(やり直し可)
- 相手が **わざと逸れ手**を指す局面では:「ここを咎めてください」とユーザーに最善手を探させ、
  エンジン/台本で正誤判定+解説
- 理想陣形(`goalSfen`)到達で「達成」演出
- 目的:定石を「自分で指せる」ようにする

---

## 5. 画面構成・UIデザイン

> **モックアップが唯一の正**:`mockup.html`(公開 Artifact)を実装時の見た目・
> 挙動の基準とする。以下はその仕様書。UX は **Airbnb アプリ風**、モバイルファースト。

### 5.0 デザインシステム(木目/和のトーン)
- **配色**:榧材の木目ベース。和紙 `#F7F1E4` / カード白 / 木肌ヒーロー `#E7C88A`系 /
  焦茶アクセント `#6E4A25` / 朱 `#B23A2E`(手番・保存) / 墨 `#2A2115`。
  金茶 `#B0842E`(なぞりガイド)。**ライト/ダーク両対応**(トークンで切替)。
- **書体**:見出し=明朝、本文=ゴシック、**駒=楷書(毛筆)系**。
  - 駒フォントは端末依存を避けるため、本実装では **駒専用フォントを同梱**する
    (教科書体/楷書系。ライセンス確認のうえ bundle)。
- **駒**:五角形シェイプ(木肌 `#F1D296`＋細い縁、くっきり黒字、成駒は朱字)。
  後手は 180°回転。将棋ウォーズ風のフラットな質感。
- **ナビゲーション**:上部タブは持たない。下部タブバー(探す/保存/学習/マイ)＋
  カードタップ＋戻るボタンで画面遷移。

### 5.1 ホーム(探す・検索)= 初期画面
- 上部に**検索バー**(戦法名でリアルタイム絞り込み)
- **カテゴリ**の横スクロール:`人気 / 初心者向け / 居飛車 / 振り飛車 / 中飛車 / 奇襲戦法`
  - 人気=全件(popularity 降順) / 初心者向け=`level` に「入門」を含む / それ以外=`category` 一致
- **Airbnb風カード一覧**:各カードに
  - ヒーロー(木肌背景＋盤の格子＋大きな戦法シンボル駒、保存ハート、準備中オーバーレイ)
  - 戦法名・かな・★人気・「N ライン ・ 難易度 ・ 先手/後手対応」
  - `ready` のカードのみタップで §5.2 へ。`ready:false` は「準備中」で遷移しない

### 5.2 対抗形・条件選択
- ヘッダ「(戦法名) vs …」。相手戦法・自分の手番(先手/後手)・モード(学習/練習)を選択
- 「この設定で始める」で §5.3 / §5.4 へ

### 5.3 学習ビュー(なぞり)
- 縦積み:盤(上)→ 解説カード → ナビ行
- 盤の上下に**持ち駒トレイ**(△上/▲下、横並び)
- **なぞりガイド**:次の手の移動先マスを**金茶で脈動**、動かす駒を**半透明ゴースト**表示、
  移動元も強調。「光っているマスへ動かして次の手をなぞる」案内
- 解説カード:現在手(▲２五歩 等)・解説文・「狙い」・分岐ナビ(本線/変化/逸れ手)
- ナビ行:最初へ / 1手戻る / なぞって次へ

### 5.4 練習ビュー(実戦)
- 盤(自分の手番だけ操作)。「▲あなたの手番」「相手が定石を外した」等のバッジ
- 「咎めてください」プロンプト → 指した手を判定
  - 外れ:✕＋理由 / 正解:✓＋**エンジン評価値付き**解説
- 分岐ナビ:エンジン解説 / もう一度 / ヒント

### 5.5 定石エディタ(開発者専用・§7.4)
- 盤で手を追加＋種別(本線/別手順/逸れ手)＋解説入力
- **定跡DBの候補手**(手・出現頻度・評価値)を提示、採用ボタン
- 「JSON で書き出す」

### 5.6 将来画面(v2+)
- 進捗/マイページ(コース別達成)、保存(お気に入り)、設定

### 5.7 主要 UI 部品(実装単位)
- `Board`(自前:盤・駒(五角形+楷書)・持ち駒トレイ・最終手/なぞりガイド/ヒント表示)
  ・`Piece`(駒1個)・合法手 dests は tsshogi から自前計算
- `StrategyCard` / `SearchBar` / `CategoryTabs`(ホーム)
- `MoveList`(棋譜と分岐ツリー) / `CommentPanel`(解説・エンジン解説) / `BranchNav`
- `BottomTabBar`

---

## 6. エンジン統合(YaneuraOu WASM)

- **Web Worker** 上で USI エンジンを起動(UI スレッドを塞がない)
- 練習モードに入るとき **遅延ロード**(WASM は重いので初回だけ)
- 用途:
  - 未知局面の評価値取得(`go` → `bestmove` / `info score`)
  - ユーザーの「咎め手」の妥当性判定(最善手/評価差で採点)
  - ユーザーが詰まったときのヒント(最善手)
- ラッパ API(例):
```ts
interface Engine {
  init(): Promise<void>;
  evaluate(sfen: string, ms: number): Promise<{ bestMove: string; scoreCp: number; pv: string[] }>;
  dispose(): void;
}
```
- 解説文は当面 **テンプレート + 評価値 + 原則タグ**で生成(自然言語 LLM は将来拡張)

---

## 7. 定石データの作成方針

### 7.1 役割分担(「監修問題」の解決)
作者(ユーザー)は将棋初心者で定石の正誤を判断できない。そこで
**手の正しさは機械が担保し、解説は Claude が自作する**分担にする。

| 何を | 誰/何が担保するか |
|---|---|
| 手順が定跡的に正しいか | **やねうら王 標準定跡DB + エンジン**(自動検証) |
| 逸れ手への最善応手 | **エンジン**(自動) |
| 「なぜこの手か」の解説文 | **Claude** が公開解説サイトで学び自作 |
| 全体の使い心地の確認 | ユーザー |

### 7.2 情報源
- **機械可読(手の正しさ)**:やねうら王 標準定跡DB(`standard_book.db`。SFEN+指し手+
  評価値+出現頻度)/ ShogiBookTools / エンジンによる検証
  - ⚠️ **要確認**:定跡DB・エンジンのライセンス(再配布/商用条件)。
    個人学習用は可の見込みだが、公開前に必ず確認する
- **人間向け解説(Claude の学習用リファレンス。丸写し厳禁)**:
  将棋ルール.com / ゼロから始める将棋研究所 / 将棋DB2 / 将棋ロード 等
- **著作権対策**:定石書・解説サイトの文章は丸写ししない。
  局面(SFEN)+手順(USI)は事実データ、解説は **自作** で構成

### 7.3 作成フロー
1. やねうら王定跡DB/エンジンで、その局面の定跡手・評価値・頻度を取得
2. 定石エディタ(§7.4)上で本線・別手順・代表的逸れ手を組み立てる
3. 各手・各局面に Claude が解説文を付ける
4. ライン全体をエンジンで再検証(悪手が本線に混ざっていないか)
5. JSON をエクスポート → `src/data/joseki/` へ

### 7.4 定石エディタ(開発用ツール・**作成する**)
SFEN/USI を手書きする JSON 編集はミスの温床。そこで **盤で指しながら
定石データを作る開発専用画面**を用意する(ユーザー向け画面ではない)。

- 盤上でマウスで手を指す(=普通に指す感覚で分岐を作る)
- 各局面に解説文をテキスト入力
- やねうら王定跡DB/エンジンと連携し「この局面の定跡手候補・評価値」を表示
- 手の種別(main / alt / deviation)を指定
- 完成したツリーを **正しい JSON として自動エクスポート**

### 7.5 v1 の具体コンテンツ(居飛車 vs 四間飛車、居飛車側)
- 急戦系(例:斜め棒銀 or ４五歩早仕掛け)の本線 → 理想陣形まで
- 持久戦系(例:居飛車穴熊)の本線 → 理想陣形まで
- 各ラインに代表的な逸れ手 2〜3 個 + 咎め方

---

## 8. ディレクトリ構成(案)

```
shogi-joseki/
  public/
    engine/                 # yaneuraou.wasm 本体
  src/
    domain/                 # tsshogi ラッパ・型(JosekiCourse 等)
    data/joseki/            # 定石 JSON コース
    engine/                 # YaneuraOu Worker ラッパ
    ui/                     # Board / MoveList / CommentPanel ...
    features/
      strategy-select/
      matchup-select/
      learn/
      practice/
      editor/               # 開発用 定石エディタ
    store/                  # Zustand
    App.tsx / main.tsx
  DESIGN.md
```

---

## 9. 実装フェーズ(この順で進める)

| Phase | 内容 | 完了条件 |
|---|---|---|
| 0 | 雛形 + 盤描画 + 合法手 | tsshogi+shogiground で自由に駒を動かせる |
| 1 | データモデル + 学習モード(なぞり) | 居飛車 vs 四間飛車 本線 1 本を解説付きでなぞれる |
| 2 | 分岐ナビ + 逸れ手(台本の咎め) | alt/deviation を切替表示できる |
| 3 | エンジン統合 + 逸れ手フォールバック + 練習モード | 未知手にエンジン応手・自分の手番で練習できる |
| 4 | 定石エディタ + ライン増強 | 急戦/持久戦の本線+逸れが揃う |
| 5 | 進捗永続化(localStorage)/ 戦法・対抗形の追加 | 複数コースを切替できる |

> 各 Phase 着手前に、そのフェーズの具体変更案を提示して合意を取る(方針確認ルール)。

---

## 10. リスクと対策

| リスク | 対策 |
|---|---|
| 定石の正確性・検証コスト | エンジン検証 + 定石エディタで作成負担を下げる |
| WASM エンジンのサイズ/性能 | Web Worker + 練習モードでの遅延ロード |
| 逸れ手解説の質(テンプレの限界) | v1 はテンプレ+評価値。将来 LLM 解説を検討 |
| 対抗形のスコープ膨張 | v1 は 1 対抗形厳守。データ追加で段階拡張 |
| ライブラリの API 差異 | 実装前に tsshogi/shogiground/yaneuraou.wasm を実検証 |

---

## 11. 将来拡張(v2 以降のメモ)
- 四間飛車側の手順、他の対抗形(相居飛車:角換わり/矢倉、相振り飛車 等)
- LLM による自然言語の逸れ手解説
- 進捗・弱点分析、間違えた分岐の復習(SRS)
- Firebase 連携(アカウント/クラウド同期)
- Capacitor でモバイル化
